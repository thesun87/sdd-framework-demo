{
  "name": "sdd-framework-demo",
  "version": "0.1.0",
  "private": true,
  "description": "Agentic SDD Protocol v2 — template repository and glue layer",
  "type": "module",
  "engines": { "node": ">=20.12" },
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "test:regression": "node --test \"tests/**/*.test.mjs\"",
    "lint": "node scripts/lint.mjs",
    "build": "node -e \"console.log('no build step — glue layer is interpreted')\""
  },
  "license": "UNLICENSED"
}
