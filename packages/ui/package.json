{
  "name": "ui",
  "version": "0.1.0",
  "private": true,
  "description": "Thành phần UI dùng chung — dựng khung ở T001, chưa có mã sản phẩm.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "lint": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "vitest": "5.0.1",
    "jsdom": "30.1.0",
    "@testing-library/react": "16.3.3"
  }
}
