{
  "name": "shared",
  "version": "0.1.0",
  "private": true,
  "description": "Kiểu và schema dùng chung giữa các workspace — dựng khung ở T001.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "lint": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "zod": "4.6.5"
  },
  "devDependencies": {
    "vitest": "5.0.1"
  }
}
